# Superseded: per-repo Hugging Face Space

API hosting moved to the shared portfolio ML API.

| | |
|---|---|
| GitHub | [aalias01/portfolio-ml-api](https://github.com/aalias01/portfolio-ml-api) |
| Hugging Face | [alvinalias/portfolio-ml-api](https://huggingface.co/spaces/alvinalias/portfolio-ml-api) |
| This service | `https://alvinalias-portfolio-ml-api.hf.space/retail` |

To update the live API after changing `api/`, `src/`, or `models/` here, run `scripts/sync_from_portfolio.sh` in the portfolio workspace `portfolio_ml_api` repo and push to `main`.

The per-repo HF Space (`retail-returns-intelligence`) and GitHub sync workflow were removed.
