name: Wake Retail HF Space

on:
  schedule:
    - cron: "17 */12 * * *"
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -fsS https://alvin-a-retail-returns-intelligence.hf.space/health
